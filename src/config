#default plim configuration, warning PARSER of this configuration isn`t completed.

plim {
	textbuffer.lines {
		use_msg_levels = "false"

		first = "+c2,3 +u test +time +b<+user>:+r "
		last = "+c 2,3+u+time test +b<+user>:+r "
		slib = "+b+time+c 4,0 <Cloudcat>+r:  "
	}

	statusbar {
		time = "+time"
		nick = "+nick"
		server = "+server"
		windows = "+windows"
	}

}
