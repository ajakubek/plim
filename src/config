#default plim configuration, warning PARSER of this configuration isn`t completed.

plim {
	textbuffer.lines {
		use_msg_levels = "false"
		first = "%time <%user>: "
#if use_msg_levels = true
		last = "%time <%user>: "
		slib = "%time <%user>: "
#endif

	}

	statusbar {
		time = "%time"
		nick = "%nick"
		server = "%server"
		windows = "%windows"
	}

}